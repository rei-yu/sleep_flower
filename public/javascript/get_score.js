function get_score (id) {
  if (id === 1 || id === 2 || id === 3) {
    score = 80;
  } else if (id === 4) {
    score = 80;
  } else if (id === 5) {
    score = 80;
  } else if (id === 6) {
    score = 80;
  } else {
    score = 100;
  }
  return score;
}
